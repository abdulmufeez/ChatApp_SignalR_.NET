@model IEnumerable<User>


@foreach (var user in Model)
{
    <div>
        <a class="room-botton" asp-controller="Chat" asp-action ="CreatePrivateRoom" asp-route-userId="@user.Id">@user.UserName</a>
    </div>
}